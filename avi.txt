async function fillByText(page, text: string, value: string) {
  const locator = page.locator('input, textarea').filter({
    has: page.getByLabel(new RegExp(`^${text}$`, 'i'))
  }).or(
    page.getByPlaceholder(new RegExp(`^${text}$`, 'i'))
  );

  await locator.fill(value);
}
