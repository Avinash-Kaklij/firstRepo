if (matchingHarEntry) {
        // Extract the path for the response data from the HAR entry
        const responseFilePath = path.join(folderPath, matchingHarEntry.response.path);

        // Load the response data from the folder
        const responseFileData = loadJsonFile(responseFilePath);

        // Return the saved response data (no modification of status or headers)
        return responseFileData;
    }
